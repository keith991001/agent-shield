# Built binary
/agent-shield

# Go
*.exe
*.test
*.prof
*.out
go.work
go.work.sum

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local env
.env
.env.local

# Runtime artifacts (SQLite archive, recordings) — ignore at the
# top level only. Assets meant for the README live in assets/ and
# are tracked explicitly.
/*.db
/*.db-wal
/*.db-shm
/*.sqlite
/*.sqlite3
/*.mov
/*.mp4
/*.webm
# Top-level GIFs are recordings; assets/*.gif is intentional and
# tracked.
/*.gif
